
import { position } from 'track-application/helpers/position';
import { module, test } from 'qunit';

module('Unit | Helper | position');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = position([42]);
  assert.ok(result);
});

