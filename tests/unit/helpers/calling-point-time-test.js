
import { callingPointTime } from 'track-application/helpers/calling-point-time';
import { module, test } from 'qunit';

module('Unit | Helper | calling point time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = callingPointTime([42]);
  assert.ok(result);
});

