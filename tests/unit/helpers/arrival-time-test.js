
import { arrivalTime } from 'track-application/helpers/arrival-time';
import { module, test } from 'qunit';

module('Unit | Helper | arrival time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrivalTime([42]);
  assert.ok(result);
});

