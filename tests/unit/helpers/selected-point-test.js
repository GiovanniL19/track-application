
import { selectedPoint } from 'track-application/helpers/selected-point';
import { module, test } from 'qunit';

module('Unit | Helper | selected point');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = selectedPoint([42]);
  assert.ok(result);
});

