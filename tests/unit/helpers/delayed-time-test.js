
import { delayedTime } from 'track-application/helpers/delayed-time';
import { module, test } from 'qunit';

module('Unit | Helper | delayed time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = delayedTime([42]);
  assert.ok(result);
});

