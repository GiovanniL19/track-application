
import { isCallingPointDelayed } from 'track-application/helpers/is-calling-point-delayed';
import { module, test } from 'qunit';

module('Unit | Helper | is calling point delayed');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isCallingPointDelayed([42]);
  assert.ok(result);
});

